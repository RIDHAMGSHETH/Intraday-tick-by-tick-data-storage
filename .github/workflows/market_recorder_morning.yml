name: "Market Recorder - Morning Session (NSE & MCX Day)"

on:
  schedule:
    # 03:40 UTC = 09:10 AM IST (Monday to Friday)
    - cron: '40 3 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  record-morning-market:
    runs-on: ubuntu-latest
    timeout-minutes: 355

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Morning Session (NSE 09:15-15:30 IST)
        env:
          KOTAK_CONSUMER_KEY: ${{ secrets.KOTAK_CONSUMER_KEY }}
          KOTAK_MOBILE_NUMBER: ${{ secrets.KOTAK_MOBILE_NUMBER }}
          KOTAK_UCC: ${{ secrets.KOTAK_UCC }}
          KOTAK_MPIN: ${{ secrets.KOTAK_MPIN }}
          KOTAK_TOTP_SECRET: ${{ secrets.KOTAK_TOTP_SECRET }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_REPO_ID: ${{ secrets.HF_REPO_ID }}
        run: |
          python engine/run_cloud_session.py --session morning --max-minutes 345

      - name: Commit & Push 1-Minute Bars to GitHub
        if: always()
        run: |
          git config user.name "MarketRecorderBot"
          git config user.email "bot@marketrecorder.internal"
          git add data/*/20*/*1min_bars*.csv data/*/20*/*tape*.csv || true
          if git diff --staged --quiet; then
            echo "No new data to commit."
          else
            git commit -m "Auto-record 1-min bars & tape for $(date +'%Y-%m-%d') [Morning Session]"
            git pull --rebase origin main || true
            git push origin main
          fi
